# Set to your public HTTPS origin before a production build.
# The site is served at the origin root; do not include a path or trailing query.
# Leave empty for local development. Canonical/share-image URLs are then omitted.
VITE_SITE_URL=
